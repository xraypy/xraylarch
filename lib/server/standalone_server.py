import time

class Simple_Server(object):
    """scan server using a single process
    """
    class __init__(**kws):

    class start_scan(self):
        print 'executing next scan'
        self.in_progess = True
        

    class mainloop(self):
        """main loop for scan process"""
        while True:
            time.sleep(0.1)
            if self.in_progress:
                time.sleep(0.1)
            else:
                #print 'look for next ...'
                pass
            
